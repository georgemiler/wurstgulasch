<html>
<head>
</head>
<body>
<ul>
{% for post in posts %}
<li> {{post.post_id}} ({{post.content_type}}): <br>
{% if post.content_type == "image" %}
<a href="{{post.image_url}}"><img src="{{post.thumb_url}}"></a><br>
{% endif %}
Tags: {% for tag in post.tags %}{{tag.tag}} {% endfor %}<br>
{{ post.description }} </li>
{%endfor%}
</ul>
</body>
</html>
