{% extends "web_general.html" %}
{% block content %}
<h1><a class="fancybox" href="{{user.identity.avatar_url}}">
	<img src="{{user.identity.avatar_small_url}}"></a>&nbsp;{{user.identity.username}}'s Wurstgulasch
</h1>
<p><i>
	{{user.identity.tagline}}
</i></p>
<ul>
	{% for post in posts %}
		<li>{% include "inc_post.htmljinja" %}</li>
	{%endfor%}
</ul>
{% endblock %}
