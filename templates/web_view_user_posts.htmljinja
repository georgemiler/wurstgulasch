{% extends "web_general.html" %}
{% block content %}
<h1><a class="fancybox" href="{{user.avatar_url}}">
	<img src="{{user.avatar_small_url}}"></a>&nbsp;{{user.name}}'s Wurstgulasch
</h1>
<p><i>
	{{user.tagline}}
</i></p>
<ul>
	{% for post in posts %}
		<li>{% include "inc_post.htmljinja" %}</li>
	{%endfor%}
</ul>
{% endblock %}
